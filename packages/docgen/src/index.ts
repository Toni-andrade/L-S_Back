/**
 * Proposal PPTX + Portuguese email generation (pptxgenjs). Built in Phase 3.
 *
 * Reminders baked into the spec:
 * - Calibri, Brazilian number formatting, tables over pie/donut charts.
 * - pptxgenjs mutates shadow option objects in place: always use factory
 *   functions returning fresh shadow objects, never a shared constant.
 * - No gold/yellow, no em/en dashes, no "É com satisfação", no 35.000 references.
 */
export const DOCGEN_PACKAGE_PHASE = 3;
